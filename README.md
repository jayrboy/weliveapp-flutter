# weliveapp

A new Flutter project.

# build & deploy
flutter build web --release

npm install -g firebase-tools
firebase login:ci

# firebase.json
'build/web'

firebase init hosting
firebase deploy

